import { Outlet } from 'react-router-dom'

/** Contenedor para /app/reports/* — el índice es el hub; las rutas hijas son páginas completas. */
export default function ReportsSectionLayout() {
  return <Outlet />
}
